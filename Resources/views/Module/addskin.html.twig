{% extends 'TerrificComposerBundle::base.html.twig' %}

{% block content %}

<div class="sidebar">
    <ul>
        <li><a href="{{ path('composer_create_module') }}" class="icon module"><span>Module</span></a></li>
        <li class="active"><a href="{{ path('composer_add_skin') }}" class="icon skin"><span>Skin</span></a></li>
        <li><a href="{{ path('composer_create_page') }}" class="icon page"><span>Page</span></a></li>
    </ul>
</div>
<div class="content">
    {% if app.session.hasFlash('notice') %}
    <div class="notice">
        {{ app.session.flash('notice') }}
    </div>
    {% endif %}
    <form novalidate="novalidate" action="{{ path('composer_create_module') }}" method="post" {{ form_enctype(form) }}>


        <div class="actions">
            <input class="button" type="submit" name="finish" value="Finish" />
            <input class="button addSkin" type="button" name="skin" value="Add Another Skin" />
        </div>
    </form>
    <span class="loader"><img src="/bundles/terrificcomposer/images/loader.gif" alt="" /></span>
</div>

{% endblock %}