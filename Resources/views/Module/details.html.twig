{% extends layout %}

{% block composition %}

{% if app.session.flashbag.has('notice') %}
    <div class="notice">
        {{ app.session.flashbag.get('notice')[0] }}
    </div>
{% endif %}

{% if module and template %}
    {{ tc.module(module, template, skins )}}
{% endif %}

{% endblock %}

